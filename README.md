# -_proyecto_java_registroClientes_con_XML_Oracle-19C_- :. / .
📚 Proyecto Java – Registro de Clientes con XML y Oracle 19c:

<img width="1024" height="1024" alt="image" src="https://github.com/user-attachments/assets/14f68417-8c12-442f-81aa-40d8613240f9" />  

Proyecto académico desarrollado en Java (IntelliJ IDEA) que implementa :

- ✔ Registro de clientes

- ✔ Consumo de datos desde XML (ingresos económicos)

- ✔ Persistencia en Oracle 19c

- ✔ Arquitectura organizada para proyectos universitarios

## 🏗 Arquitectura Utilizada:

El proyecto sigue una arquitectura por capas:
- Modelo
- DAO
- Servicio
- XML Parser
- UI (Consola o Swing)

```

Base de Datos Oracle 19c:
📊 1. Modelo de Datos – Oracle 19c:

📦 Tabla CLIENTE
CREATE TABLE CLIENTE (
    ID_CLIENTE NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE VARCHAR2(100) NOT NULL,
    DOCUMENTO VARCHAR2(20) UNIQUE,
    TELEFONO VARCHAR2(20),
    EMAIL VARCHAR2(100),
    FECHA_REGISTRO DATE DEFAULT SYSDATE
);
💰 Tabla INGRESO_ECONOMICO
CREATE TABLE INGRESO_ECONOMICO (
    ID_INGRESO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ID_CLIENTE NUMBER,
    SALARIO NUMBER(10,2),
    OTROS_INGRESOS NUMBER(10,2),
    TOTAL_INGRESOS NUMBER(10,2),
    FECHA_CARGA DATE DEFAULT SYSDATE,
    
    CONSTRAINT FK_CLIENTE
    FOREIGN KEY (ID_CLIENTE)
    REFERENCES CLIENTE(ID_CLIENTE)
);

📊 Relación entre Tablas
CLIENTE
   │
   │ 1
   │
   └─────────── ∞
           INGRESO_ECONOMICO

📌 Interpretación

Un cliente puede tener múltiples registros de ingresos económicos.

📂 2. Estructura del Proyecto IntelliJ

registro_clientes_xml
src
 ├── config
 │      ConexionOracle.java
 │
 ├── model
 │      Cliente.java
 │      IngresoEconomico.java
 │
 ├── dao
 │      ClienteDAO.java
 │      IngresoDAO.java
 │
 ├── service
 │      ClienteService.java
 │
 ├── xml
 │      XMLIngresoParser.java
 │
 └── ui
        Main.java

⚙ 3. Conexión a Oracle

📄 config/ConexionOracle.java

package config;

import java.sql.Connection;
import java.sql.DriverManager;

public class ConexionOracle {

    private static final String URL = "jdbc:oracle:thin:@localhost:1521/XEPDB1";
    private static final String USER = "system";
    private static final String PASS = "oracle";

    public static Connection conectar() {
        try {
            return DriverManager.getConnection(URL, USER, PASS);
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }
}

👤 4. Modelo Cliente

📄 model/Cliente.java

package model;

public class Cliente {

    private int idCliente;
    private String nombre;
    private String documento;
    private String telefono;
    private String email;

    public Cliente(){}

    public Cliente(String nombre, String documento, String telefono, String email) {
        this.nombre = nombre;
        this.documento = documento;
        this.telefono = telefono;
        this.email = email;
    }

    public int getIdCliente() { return idCliente; }
    public void setIdCliente(int idCliente) { this.idCliente = idCliente; }

    public String getNombre() { return nombre; }
    public void setNombre(String nombre) { this.nombre = nombre; }

    public String getDocumento() { return documento; }
    public void setDocumento(String documento) { this.documento = documento; }

    public String getTelefono() { return telefono; }
    public void setTelefono(String telefono) { this.telefono = telefono; }

    public String getEmail() { return email; }
    public void setEmail(String email) { this.email = email; }
}

💰 5. Modelo Ingreso Económico

📄 model/IngresoEconomico.java

package model;

public class IngresoEconomico {

    private int idCliente;
    private double salario;
    private double otrosIngresos;
    private double total;

    public IngresoEconomico(){}

    public IngresoEconomico(int idCliente, double salario, double otrosIngresos) {
        this.idCliente = idCliente;
        this.salario = salario;
        this.otrosIngresos = otrosIngresos;
        this.total = salario + otrosIngresos;
    }

    public int getIdCliente() { return idCliente; }

    public double getSalario() { return salario; }

    public double getOtrosIngresos() { return otrosIngresos; }

    public double getTotal() { return total; }
}

🗄 6. DAO Cliente

📄 dao/ClienteDAO.java

package dao;

import config.ConexionOracle;
import model.Cliente;

import java.sql.Connection;
import java.sql.PreparedStatement;

public class ClienteDAO {

    public void registrarCliente(Cliente cliente){

        String sql = """
                INSERT INTO CLIENTE
                (NOMBRE,DOCUMENTO,TELEFONO,EMAIL)
                VALUES (?,?,?,?)
                """;

        try(Connection conn = ConexionOracle.conectar();
            PreparedStatement ps = conn.prepareStatement(sql)){

            ps.setString(1,cliente.getNombre());
            ps.setString(2,cliente.getDocumento());
            ps.setString(3,cliente.getTelefono());
            ps.setString(4,cliente.getEmail());

            ps.executeUpdate();

            System.out.println("Cliente registrado");

        }catch(Exception e){
            e.printStackTrace();
        }
    }
}

🗄 7. DAO Ingresos

📄 dao/IngresoDAO.java

package dao;

import config.ConexionOracle;
import model.IngresoEconomico;

import java.sql.Connection;
import java.sql.PreparedStatement;

public class IngresoDAO {

    public void guardarIngreso(IngresoEconomico ingreso){

        String sql = """
                INSERT INTO INGRESO_ECONOMICO
                (ID_CLIENTE,SALARIO,OTROS_INGRESOS,TOTAL_INGRESOS)
                VALUES (?,?,?,?)
                """;

        try(Connection conn = ConexionOracle.conectar();
            PreparedStatement ps = conn.prepareStatement(sql)){

            ps.setInt(1,ingreso.getIdCliente());
            ps.setDouble(2,ingreso.getSalario());
            ps.setDouble(3,ingreso.getOtrosIngresos());
            ps.setDouble(4,ingreso.getTotal());

            ps.executeUpdate();

            System.out.println("Ingresos guardados");

        }catch(Exception e){
            e.printStackTrace();
        }
    }
}

📄 8. XML de Ingresos

📄 ingresos.xml

<ingresos>
    <cliente>
        <idCliente>1</idCliente>
        <salario>2500000</salario>
        <otrosIngresos>500000</otrosIngresos>
    </cliente>

    <cliente>
        <idCliente>2</idCliente>
        <salario>3200000</salario>
        <otrosIngresos>700000</otrosIngresos>
    </cliente>
</ingresos>

📥 9. Lector de XML

📄 xml/XMLIngresoParser.java

package xml;

import model.IngresoEconomico;
import org.w3c.dom.*;
import javax.xml.parsers.*;

import java.io.File;
import java.util.ArrayList;
import java.util.List;

public class XMLIngresoParser {

    public List<IngresoEconomico> leerXML(String ruta){

        List<IngresoEconomico> lista = new ArrayList<>();

        try{

            File file = new File(ruta);

            DocumentBuilderFactory factory =
                    DocumentBuilderFactory.newInstance();

            DocumentBuilder builder =
                    factory.newDocumentBuilder();

            Document doc = builder.parse(file);

            NodeList nodes = doc.getElementsByTagName("cliente");

            for(int i=0;i<nodes.getLength();i++){

                Element element = (Element) nodes.item(i);

                int idCliente = Integer.parseInt(
                        element.getElementsByTagName("idCliente")
                                .item(0).getTextContent());

                double salario = Double.parseDouble(
                        element.getElementsByTagName("salario")
                                .item(0).getTextContent());

                double otros = Double.parseDouble(
                        element.getElementsByTagName("otrosIngresos")
                                .item(0).getTextContent());

                lista.add(new IngresoEconomico(idCliente,salario,otros));

            }

        }catch(Exception e){
            e.printStackTrace();
        }

        return lista;
    }
}

▶ 10. Clase Principal

📄 ui/Main.java

package ui;

import dao.IngresoDAO;
import model.IngresoEconomico;
import xml.XMLIngresoParser;

import java.util.List;

public class Main {

    public static void main(String[] args) {

        XMLIngresoParser parser = new XMLIngresoParser();
        IngresoDAO ingresoDAO = new IngresoDAO();

        List<IngresoEconomico> lista =
                parser.leerXML("ingresos.xml");

        for(IngresoEconomico ingreso : lista){

            ingresoDAO.guardarIngreso(ingreso);

        }

        System.out.println("Carga XML completada");
    }
}

🧪 11. Resultado del Sistema

Flujo de funcionamiento
Registro Cliente
      │
      ▼
Cliente guardado en Oracle
      │
      ▼
Sistema lee XML
      │
      ▼
Procesa ingresos económicos
      │
      ▼
Guarda datos en Oracle

🚀 12. Posibles Mejoras (Muy Valoradas Académicamente)

Puedes mejorar el proyecto agregando:

- ✔ Interfaz gráfica Swing

- ✔ Carga automática de XML

- ✔ Validación de datos

- ✔ Reportes de ingresos

- ✔ Gráficos estadísticos (JFreeChart)

- ✔ Stored Procedure en Oracle / .
